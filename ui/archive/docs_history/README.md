# Archived docs history

This directory preserves historical design-wave and rewrite-era document lineage that no longer belongs in the mainline `docs/` root.

Current payload notes:
- Mainline operator/runtime documentation stays under `docs/`.
- Historical wave-by-wave notes, acceptance-era deltas, and superseded convergence writeups should be archived here instead of leaking back into the root docs surface.
- Repository gates use this directory as the canonical landing zone for retained documentation history.
