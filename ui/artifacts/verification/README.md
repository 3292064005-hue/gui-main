# Verification artifacts

This directory may contain both historical and current sandbox verification materials.

- `current_delivery_fix/` is the authoritative verification bundle for the current delivery and now includes the current delivery logs/reports.
- `sandbox_repository_proof/` is a historical sandbox proof bundle retained for traceability only.

When reviewing the current delivery, prefer the artifacts under `current_delivery_fix/`.
